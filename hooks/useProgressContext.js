import ProgressContext from "@/context/progress_context";
import { useContext } from "react";

const UseProgressContext = () => {
  return useContext(ProgressContext);
};

export default UseProgressContext;
